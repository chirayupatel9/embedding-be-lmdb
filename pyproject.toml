[project]
name = "embedding-be-lmdb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.128.0",
    "lmdb>=1.7.5",
    "timm>=1.0.24",
]
